# pfSense Configs

Export pfSense firewall rule sets and DHCP/DNS config here as you build them out (Diagnostics → Backup & Restore → Download configuration as XML).

**Before committing:** strip or redact any real credentials, WAN IP details, or license info from exported XML.
